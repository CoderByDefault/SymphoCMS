//declared all the properties required for the location
export interface Sympholights {
  
    id: string,
    name: string,
    ipAddress: string,
    serverLogin: string,
    serverPassword: string,
    sympAddress: string,
    longitude: string,
    latitude: string,
    status: string   
} 